# Terragrunt's local download and generated-file cache. Always safe to delete.
.terragrunt-cache

# Generated unit configurations produced when Terragrunt processes a terragrunt.stack.hcl.
# Only the stack file itself should be versioned, not what it generates.
.terragrunt-stack
