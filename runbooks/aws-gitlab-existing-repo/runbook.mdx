# Bootstrap Gruntwork Pipelines — AWS + GitLab (existing repository)

This runbook installs [Gruntwork Pipelines](https://docs.gruntwork.io/2.0/docs/pipelines/concepts/overview)
(Terragrunt Scale) into an **existing** `infrastructure-live` repository hosted on **GitLab**, targeting
one **AWS** account.

It provisions, per account, the OIDC trust that lets your GitLab CI/CD jobs authenticate to AWS with no
long-lived credentials:

- an IAM OpenID Connect provider for GitLab CI,
- an IAM role for `terragrunt plan` (read-only, used on merge requests), and
- an IAM role for `terragrunt apply` (used on merges to your deploy branch).

Wherever a value can be discovered automatically, this runbook does it for you — your AWS account ID and
partition come from your authenticated identity. GitLab has no numeric immutable-claim mechanism, so the
OIDC trust is scoped by **group** and **project** name; you supply those (and the other non-derivable
settings) in the form.

<Admonition
  type="warning"
  title="This provisions real cloud resources"
  description="The bootstrap step creates IAM roles, an OIDC provider, and an S3 state bucket in the AWS account you authenticate to. Run this only against an account you own and intend to manage with Pipelines."
/>

<Admonition
  type="note"
  title="Prerequisite: configure SCM access for Pipelines"
  description="Gruntwork Pipelines needs access to your GitLab project to fetch your IaC code, reusable CI/CD code, and the Pipelines binary itself. Before you begin, set this up by configuring [machine users](https://docs.gruntwork.io/2.0/docs/pipelines/installation/viamachineusers) with Personal Access Tokens (PATs). This is separate from the GitLab sign-in this runbook uses to clone the project and open the merge request."
/>

## 1. Pre-flight checks

Make sure the tools this runbook drives are installed.

<Check
  id="check-prerequisites"
  title="Check required CLIs"
  description="Verifies **git**, **mise**, **jq**, and the **AWS CLI** are installed and on your PATH."
  path="checks/check-prerequisites.sh"
/>

<Admonition
  type="note"
  title="Restart after installing prerequisites"
  description="If any tool above was missing and you installed it just now, restart this runbook (reopen it) so it picks up the newly installed tools on your PATH."
/>

## 2. Authenticate to GitLab

<GitLabAuth
  id="gitlab-auth"
  title="GitLab authentication"
  description="Used to clone your repository, push the setup branch, and open the merge request. Establishes the session GITLAB_TOKEN that `git` reuses."
/>

## 3. Authenticate to AWS

Authenticate to the **AWS account you are bootstrapping**. Confirm the account shown is the one you
intend to manage — the bootstrap step will create resources there.

<AwsAuth
  id="aws-auth"
  title="AWS authentication"
  description="Credentials for the account that will host the OIDC provider and the plan/apply IAM roles."
/>

## 4. Select your infrastructure-live repository

Choose the existing repository you want to add Pipelines to — an existing local checkout, or a fresh clone.

Pipelines deploys from a single branch. Name it now — the repository is selected on that branch, and
the setup merge request is opened against it.

<Inputs id="deploy" title="Deploy branch">
```yaml
variables:
  - name: DeployBranch
    type: string
    description: The branch Pipelines deploys from. Merges here trigger apply; this branch is granted the apply role.
    default: main
    validations:
      - required
```
</Inputs>

<GitClone
  id="clone"
  inputsId="deploy"
  title="Select or clone the repository"
  description="Point this at your existing infrastructure-live repository. If you already have it checked out locally, choose **Use local checkout** and select that directory; otherwise switch to the clone source and browse to it."
  gitAuthId="gitlab-auth"
  prefilledLocalPath="infrastructure-live"
  prefilledRef="{{ .inputs.DeployBranch }}"
  source="local"
/>

<Admonition
  type="note"
  title="Local checkouts: be on your deploy branch"
  description="If you chose **Use local checkout**, make sure that checkout is on your deploy branch before continuing. The setup request opens against the branch the checkout is on; a fresh clone is put on the deploy branch for you."
/>

Confirm the repository already has a `root.hcl` at its root (the account template does not create one).

<Check
  id="check-root-hcl"
  title="Check for root.hcl"
  description="An existing Terragrunt Scale repo should already have a root.hcl configuring the S3 backend."
  path="checks/check-root-hcl.sh"
/>

## 5. Auto-derive your AWS account identifiers

This step discovers your AWS account ID and partition so you don't have to enter them by hand. Your
GitLab group and project names cannot be reliably derived, so you provide those in the form below.

<Command
  id="derive-aws"
  title="Resolve AWS account ID & partition"
  description="Reads your confirmed AWS identity to derive the account ID and partition (aws / aws-us-gov / aws-cn)."
  awsAuthId="aws-auth"
  path="scripts/derive-aws.sh"
/>

## 6. Configure the bootstrap

Fill in the values that cannot be derived automatically. Everything else is supplied for you from the
steps above.

<Inputs id="config" title="Bootstrap settings">
```yaml
variables:
  - name: AccountName
    type: string
    description: A short name for this AWS account (e.g. "dev", "prod"). Becomes the top-level directory and the Pipelines environment name.
    validations:
      - required
    x-section: Account

  - name: AWSRegion
    type: string
    description: The AWS region used for provider configuration and API calls when managing global resources.
    default: us-east-1
    validations:
      - required
    x-section: Account

  - name: GitLabGroupName
    type: string
    description: The GitLab group (or top-level namespace) that owns the project. Only CI jobs in this group/project may assume the IAM roles.
    validations:
      - required
    x-section: GitLab

  - name: GitLabProjectName
    type: string
    description: The GitLab project name (the repository's path within the group).
    validations:
      - required
    x-section: GitLab

  - name: StateBucketName
    type: string
    description: Name of the S3 bucket for OpenTofu state. Must be globally unique and obey S3 naming rules.
    validations:
      - required
    x-section: State

  - name: OIDCResourcePrefix
    type: string
    description: Prefix for the OIDC IAM resources. Creates <prefix>-plan and <prefix>-apply roles.
    default: pipelines
    validations:
      - required
    x-section: OIDC

  - name: TerragruntScaleCatalogRef
    type: enum
    description: Which terragrunt-scale-catalog version this bootstrap pins. Leave it on latest to use the newest release, resolved when the generate step runs. Pick a tag instead to pin one explicitly.
    # Regenerated by scripts/update-catalog-refs.sh -- do not hand-edit.
    options:
      - latest
      - v1.13.2
      - v1.13.1
      - v1.13.0
      - v1.12.2
      - v1.12.1
      - v1.12.0
      - v1.11.3
      - v1.11.2
      - v1.11.1
      - v1.11.0
    default: latest
    validations:
      - required
    x-section: Advanced

  - name: TerragruntVersion
    type: string
    description: Terragrunt version to pin in the repository's .mise.toml. Optional — leave as the default unless you need a specific version.
    default: "1.0.0"
    x-section: Advanced

  - name: OpenTofuVersion
    type: string
    description: OpenTofu version to pin in the repository's .mise.toml. Optional.
    default: "1.11.5"
    x-section: Advanced

  - name: Issuer
    type: string
    description: OIDC issuer URL override. Leave as "default" for gitlab.com; set this to your instance URL only for self-hosted GitLab (e.g. https://gitlab.example.com).
    default: "default"
    validations:
      - required
    x-section: Advanced
```
</Inputs>

## 7. Generate the bootstrap configuration

Render the `aws/gitlab/account` template into your repository. This writes
`<AccountName>/_global/bootstrap/terragrunt.stack.hcl`, `<AccountName>/account.hcl`,
`<AccountName>/_global/region.hcl`, and `.gruntwork/environment-<AccountName>.hcl`.

Before generating, resolve the latest catalog release so the bootstrap pins the newest version
automatically (unless you pinned a specific `TerragruntScaleCatalogRef` above).

<Command
  id="resolve-catalog-ref"
  title="Resolve the latest catalog release"
  description="Runs `git ls-remote` to find the newest terragrunt-scale-catalog release tag, used as the catalog ref for the generate step unless you pinned one."
  path="scripts/resolve-catalog-ref.sh"
/>

<Command
  id="generate-bootstrap"
  title="Render the account template"
  description="Runs `boilerplate` non-interactively with your form values and the auto-derived AWS identifiers."
  inputsId={["deploy", "config"]}
  path="scripts/generate-bootstrap.sh"
/>

Now install the Terragrunt and OpenTofu versions pinned for the repository:

<Command
  id="install-deps"
  title="Install Tool Dependencies"
  description="Runs \`mise install\` against the repository's pinned \`.mise.toml\` to install Terragrunt, OpenTofu, and any cloud CLIs. boilerplate ships with the Runbooks app (the app points scripts at it via BOILERPLATE_BIN), so it is not installed here."
  inputsId={["deploy", "config"]}
  path="scripts/install-deps.sh"
/>

## 8. Add the GitLab CI workflow

An existing repository needs the Pipelines CI workflow added explicitly (the account template does not
include it). This writes `.gitlab-ci.yml` at the repo root, including the Pipelines CI component from
your GitLab instance.

<TemplateInline
  id="workflow-file"
  generateFile={true}
  target="worktree"
  outputPath=".gitlab-ci.yml"
>
```yaml
include:
  - component: $CI_SERVER_FQDN/gruntwork-io/pipelines-workflows/pipelines@v2
```
</TemplateInline>

## 9. Provision the AWS resources

Plan first, review the output, then apply. Apply creates real resources (OIDC provider, IAM roles, and
the S3 state bucket via `--backend-bootstrap`).

<Command
  id="bootstrap-plan"
  title="Plan the bootstrap stack"
  description="Runs `terragrunt run --all ... --backend-bootstrap plan` in the bootstrap directory."
  inputsId={["deploy", "config"]}
  awsAuthId="aws-auth"
  path="scripts/bootstrap-plan.sh"
  timeoutMs={1800000}
/>

<Command
  id="bootstrap-apply"
  title="Apply the bootstrap stack"
  description="Runs `terragrunt run --all ... apply`. Creates the OIDC provider, plan/apply IAM roles, and state bucket."
  inputsId={["deploy", "config"]}
  awsAuthId="aws-auth"
  path="scripts/bootstrap-apply.sh"
  timeoutMs={1800000}
/>

## 10. Open a merge request

Commit the generated configuration, push a setup branch, and open a merge request against your
repository. Opening the MR does not start a Pipelines run — the commit is tagged `[skip ci]`, and you have already applied the bootstrap locally. After it merges to your deploy branch, Pipelines takes over.

<GitLabMergeRequest
  id="merge-request"
  title="Open the setup merge request"
  description="Creates the setup branch, commits the generated files, pushes, and opens the merge request against your deploy branch."
  gitAuthId="gitlab-auth"
  inputsId={["deploy", "config"]}
  prefilledBranchName="add-gruntwork-pipelines-{{ .inputs.AccountName }}"
  prefilledCommitMessage="Add Gruntwork Pipelines bootstrap for {{ .inputs.AccountName }} [skip ci]"
  prefilledPullRequestTitle="Add Gruntwork Pipelines bootstrap for {{ .inputs.AccountName }} [skip ci]"
  prefilledPullRequestDescription="Adds the Gruntwork Pipelines configuration for the {{ .inputs.AccountName }} environment, generated with a Gruntwork Runbook. The bootstrap stack was already applied locally, so the commit carries `[skip ci]` and Pipelines takes over once this merges."
/>

## 11. Post-flight checks

<Check
  id="verify-oidc-roles"
  title="Verify the IAM roles exist"
  description="Confirms the <prefix>-plan and <prefix>-apply IAM roles were created in the account."
  inputsId={["deploy", "config"]}
  awsAuthId="aws-auth"
  path="checks/verify-oidc-roles.sh"
/>

<Check
  id="verify-config"
  title="Verify generated files"
  description="Confirms the CI workflow, environment HCL, and bootstrap stack files are present in the repo."
  inputsId={["deploy", "config"]}
  path="checks/verify-config.sh"
/>

**You're done.** The setup merge request was committed with `[skip ci]`, so opening it does **not** kick off a Pipelines run — you applied this bootstrap locally on purpose, so Pipelines never runs against half-configured infrastructure. Once you **merge** it to `{{ .inputs.DeployBranch }}`, Gruntwork Pipelines is activated: from then on it runs `plan` on merge requests and `apply` on merges to `{{ .inputs.DeployBranch }}` for changes under `{{ .inputs.AccountName }}/`. To add another account later, re-run this runbook
and choose a new `AccountName`.
